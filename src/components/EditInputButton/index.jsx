/* eslint-disable jsx-a11y/control-has-associated-label */
import './EditInputButton.css';

const EditInputButton = ({ onClick }) => (
	<button className="edit-input-button" type="button" onClick={onClick} />
);

export default EditInputButton;
